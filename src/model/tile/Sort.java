package model.tile;

import model.player.Player;

import java.util.Collections;


public class Sort { //only AI
    //숫자별로 정렬 (7, 8, 9 | 1, 2, 3)
    public static void sort(Player AI){
        //오름차순 정렬 (1, 2, 3 | 7, 8, 9)
//        Collections.sort(AI.tileList);
    }
}